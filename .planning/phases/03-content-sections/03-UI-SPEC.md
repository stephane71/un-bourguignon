---
phase: 3
slug: content-sections
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-16
---

# Phase 3 — UI Design Contract

> Visual and interaction contract for the 7 content sections (Hero, A propos, Pour qui, Services, Methode, Benefices, Contact). Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (manual Tailwind CSS 4 + custom components) |
| Preset | not applicable |
| Component library | none (custom primitives: Button, Card, Section, Container) |
| Icon library | Inline SVG components in `src/components/icons/` (className + size props, aria-hidden, currentColor) |
| Font | Lora (serif, `--font-lora`) + Instrument Sans (sans-serif, `--font-instrument`) via next/font/google |

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon-text inline gaps |
| sm | 8px | Tight element spacing (list item gaps, card internal margins) |
| md | 16px | Default element spacing (paragraph gaps, card padding sub-elements) |
| lg | 24px | Card padding (existing `p-6`), grid gaps |
| xl | 32px | Stepper step gaps, inter-block spacing within sections |
| 2xl | 48px | Section heading to content gap |
| 3xl | 64px | Section vertical padding on mobile (`py-16`) |

Exceptions:
- Hero section: `py-20 sm:py-28 lg:py-32` (taller padding than standard sections for landing-area feel -- values exceed 3xl but are justified as one-off hero treatment, not reusable tokens)
- Standard sections use responsive padding from the scale: `py-16 lg:py-20` maps to 64px mobile, progressing via Tailwind responsive utilities
- Portrait image: 200px (mobile) / 280px (desktop) -- fixed dimensions, not spacing-scale values
- Min tap target: 48px on all interactive elements (phone, email, CTAs, social links)

---

## Typography

4 base font sizes, 2 weights. Responsive breakpoint variants use the same 4 base sizes at different breakpoints.

| Role | Font | Size | Weight | Line Height | Usage |
|------|------|------|--------|-------------|-------|
| Body | Instrument Sans | 16px (`text-base`) | 400 (regular) | 1.6 (`leading-relaxed`) | Paragraph text, card descriptions, sub-prestations lists |
| Label | Instrument Sans | 14px (`text-sm`) | 700 (bold) | 1.5 (`leading-snug`) | Table headers, format labels, small annotations |
| Heading | Lora | 20px (`text-xl`) | 700 (bold) | 1.3 | Card titles, step titles, benefit titles (H3 level) |
| Display | Lora | 36px (`text-4xl`) | 700 (bold) | 1.2 | Section H2 titles, Hero H1 name, step numbers |

Base size scale: 14px, 16px, 20px, 36px.
Weight scale: 400 (regular), 700 (bold).

### Role-to-class mapping

| Role | Mobile | Desktop (lg+) | Notes |
|------|--------|---------------|-------|
| Hero H1 (name) | `text-4xl` (36px) | `text-5xl` (48px) | 48px is responsive variant of Display, line-height 1.1 |
| Hero subtitle | `text-xl` (20px) | `text-2xl` (24px) | Uses Heading size, Instrument Sans 400, line-height 1.4 |
| Hero slogan | `text-xl` (20px) | `text-xl` (20px) | Uses Heading size, Lora 400 italic, line-height 1.5 |
| Section H2 | `text-xl` (20px) | `text-4xl` (36px) | Scales from Heading to Display at lg breakpoint |
| Card/Step title | `text-xl` (20px) | `text-xl` (20px) | Heading size, Lora 700 |
| Step number | `text-4xl` (36px) | `text-5xl` (48px) | Display size, Lora 700, color or-light #D4A574 |
| Body text | `text-base` (16px) | `text-base` (16px) | Instrument Sans 400 |
| Label text | `text-sm` (14px) | `text-sm` (14px) | Instrument Sans 700 |

Responsive variants (24px at `text-2xl`, 48px at `text-5xl`) are Tailwind breakpoint scaling of the 4 base sizes, not additional size declarations.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#FFFFFF` (white) | Page background, Hero/Pour qui/Methode/Contact section backgrounds |
| Secondary (30%) | `#F5F0E8` (ecru) | Alternating section backgrounds (A propos, Services, Benefices) |
| Text | `#6B4C3B` (brun) | All body text, headings, card text -- set on `body` in globals.css |
| Accent (10%) | `#D4A574` (or-light) | Step numbers in Methode stepper |
| Accent alt | `#C5973B` (or) or `#B8754C` (argile) | Service card top border accent |
| Card border | `#E8DCC8` (sable) | Card borders on white backgrounds (Pour qui cards via `on-white` variant) |
| CTA primary bg | `#6B4C3B` (brun) | "Prendre contact" button background |
| CTA primary text | `#FFFFFF` (white) | "Prendre contact" button text |

Accent reserved for:
- Methode step numbers (or-light `#D4A574`, Lora Bold, 36-48px)
- Service card top borders (or `#C5973B` or argile `#B8754C`, 3px top border)
- Never used for body text on light backgrounds (decorative only, fails WCAG AA)

---

## Section Background Alternation

| Section | Order | Background | Section `alternate` prop |
|---------|-------|------------|--------------------------|
| Hero | 1 | white | `false` |
| A propos | 2 | ecru | `true` |
| Pour qui | 3 | white | `false` |
| Services | 4 | ecru | `true` |
| Methode | 5 | white | `false` |
| Benefices | 6 | ecru | `true` |
| Contact | 7 | white | `false` |

---

## Section IDs

Must match header nav anchors exactly:

| Section | `id` attribute |
|---------|----------------|
| Hero | `hero` |
| A propos | `a-propos` |
| Pour qui | `pour-qui` |
| Services | `services` |
| Methode | `methode` |
| Benefices | `benefices` |
| Contact | `contact` |

---

## Component Inventory

### New Icons (src/components/icons/)

All follow existing pattern: `interface {Name}Props { className?: string; size?: number; }`, default size 24, viewBox 0 0 24 24, `aria-hidden="true"`, `currentColor`, stroke-based with `stroke-width="1.5"` (thin-line style for Benefices per BENE-01).

| Icon | Used In | Visual |
|------|---------|--------|
| Phone.tsx | Contact section | Handset outline |
| Mail.tsx | Contact section | Envelope outline |
| Clock.tsx | Benefices | Clock face outline (time saved) |
| Key.tsx | Benefices | Key outline (autonomy) |
| UserCheck.tsx | Benefices | Person with checkmark (single contact) |
| CoinStack.tsx | Benefices | Coin/euro outline (fair pricing) |

### New Section Components (src/components/sections/)

| Component | Requirements | Key Props |
|-----------|-------------|-----------|
| HeroSection.tsx | HERO-01 through HERO-06 | none (static content) |
| AProposSection.tsx | APRO-01, APRO-02 | none |
| PourQuiSection.tsx | POUR-01, POUR-02, POUR-03 | none |
| ServicesSection.tsx | SERV-01, SERV-02, SERV-03, SERV-04 | none |
| MethodeSection.tsx | METH-01, METH-02, METH-03, METH-04, METH-05 | none |
| BeneficesSection.tsx | BENE-01, BENE-02 | none |
| ContactSection.tsx | CONT-01, CONT-02, CONT-03, CONT-04, CONT-05 | none |

All are Server Components (no `use client`). Content is hardcoded from `content/presentation_activite.md`.

---

## Layout Contracts

### Hero (HeroSection)

- **Mobile (< lg):** Portrait circle centered (200px, `rounded-full overflow-hidden`), then text block centered below. Stack order: portrait, name (H1), subtitle, slogan (italic), CTAs.
- **Desktop (lg+):** Two-column layout. Left: text block (name H1, subtitle, slogan, CTAs) -- left-aligned. Right: portrait circle (280px). Vertically centered.
- **Portrait:** `next/image` with `width={280} height={280}`, `rounded-full`, `src="/images/photo-portrait.png"`, `alt="Stephane Maire, consultant numerique"`, `priority` attribute for LCP.
- **CTA row:** "Prendre contact" as `<Button as="a" href="#contact" variant="primary">`, "Decouvrir mes services" as a plain text link with right arrow (`→`), `font-sans font-bold text-brun underline-offset-4 hover:underline`.
- **Extra padding:** `py-20 sm:py-28 lg:py-32` instead of standard Section padding.

### A propos (AProposSection)

- Single-column text layout. H2 section title, then prose paragraphs.
- Key points (independant, relation directe, deplacement client, EI) emphasized as a `<dl>` or bold inline spans within text.
- Max prose width: `max-w-3xl` for comfortable reading line length.

### Pour qui (PourQuiSection)

- H2 section title + intro paragraph.
- 4 problem cards in grid: `grid grid-cols-1 md:grid-cols-2 gap-6`.
- Each card uses `<Card variant="on-white">` with H3 title (Lora bold) + description paragraph (Instrument Sans regular).
- No icons on cards -- text-only per decision.

### Services (ServicesSection)

- H2 section title + intro paragraph.
- 5 service cards in grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`.
- Each card uses `<Card variant="on-ecru">` (section background is ecru) with a gold/argile top border: `border-t-3 border-or` (or `border-argile`).
- Card content: H3 title (Lora bold), description paragraph, then `<ul>` bullet list of sub-prestations.
- At 3-column breakpoint, cards 4 and 5 left-aligned (natural grid flow, empty cell on right).

### Methode (MethodeSection)

- H2 section title.
- **Stepper sub-block:** 3 steps.
  - Mobile: vertical stack. Each step: step number (Lora Bold, or-light `#D4A574`, `text-4xl`) + step title (Lora bold) + description paragraph. Connector: vertical line segment between steps (`border-l-2 border-sable` or similar).
  - Desktop (lg+): horizontal 3-column layout. Each step: number on top, title, description below. Connector: horizontal line between step columns (`border-t-2 border-sable` connecting the steps).
- **Formats table sub-block:** Spaced below stepper (`mt-12 lg:mt-16`).
  - Standard HTML `<table>` with 3 columns: Format, Duree, Ideal pour.
  - Table styling: `w-full text-left`, header row in `font-serif font-bold`, rows separated by `border-b border-sable`, cell padding `py-3 px-4`.
  - Responsive: table inside a `overflow-x-auto` wrapper for small screens.

### Benefices (BeneficesSection)

- H2 section title.
- 4 benefit items in grid: `grid grid-cols-1 md:grid-cols-2 gap-8`.
- Each item: thin-line SVG icon (32px, `text-brun`) + H3 title (Lora bold) + description paragraph (Instrument Sans regular).
- Icon positioned above title (stacked layout within each grid cell).

### Contact (ContactSection)

- H2 heading: "Pret a travailler ensemble ?" (Lora bold).
- Intro line: "Pour un premier echange sans engagement, contactez-moi directement :"
- Contact blocks stacked vertically, centered, `max-w-md mx-auto`.
- **Phone block:** `<a href="tel:+33662461643">` with Phone icon + "06 62 46 16 43". Min height 48px, full-width on mobile, `bg-brun text-white rounded-lg` -- styled as primary action.
- **Email block:** `<a href="mailto:stephane-ei@un-bourguignon.com">` with Mail icon + email text. Min height 48px, `border border-brun text-brun rounded-lg` -- styled as secondary action.
- **Social links:** LinkedIn and Facebook icons (existing components) in a horizontal row below, each wrapped in `<a>` with min 48px tap target.

---

## Copywriting Contract

| Element | Copy | Source |
|---------|------|--------|
| Hero H1 | "Stephane Maire" | HERO-01 |
| Hero subtitle | "Consultant numerique de proximite" | HERO-01 |
| Hero slogan | "La methode d'un expert, la proximite d'un artisan." | HERO-02, italic |
| Primary CTA | "Prendre contact" | HERO-04, links to #contact |
| Secondary CTA | "Decouvrir mes services →" | HERO-05, text link to #services |
| A propos H2 | "Qui suis-je ?" | content source section 1 |
| Pour qui H2 | "Pour qui ?" | content source section 2 |
| Services H2 | "Ce que je propose" | content source section 3 |
| Methode H2 | "Ma methode de travail" | content source section 4 |
| Benefices H2 | "Ce que vous gagnez" | content source section 5 |
| Contact H2 | "Pret a travailler ensemble ?" | content source, 03-CONTEXT.md specific idea |
| Contact intro | "Pour un premier echange sans engagement, contactez-moi directement :" | content source |
| Empty state | Not applicable -- static content site, no dynamic data | -- |
| Error state | Not applicable -- static export, no server errors | -- |
| Destructive actions | None in this phase | -- |

All section body copy is sourced verbatim from `content/presentation_activite.md`. Accented characters (e, a, etc.) must be preserved in the actual implementation.

---

## Interaction Contracts

| Interaction | Behavior |
|-------------|----------|
| "Prendre contact" CTA | Smooth scroll to `#contact` section (CSS `scroll-behavior: smooth` already configured) |
| "Decouvrir mes services" link | Smooth scroll to `#services` section |
| Phone number tap | Opens device dialer via `tel:+33662461643` |
| Email tap | Opens mail client via `mailto:stephane-ei@un-bourguignon.com` |
| LinkedIn/Facebook tap | Opens external URL in same tab (consistent with footer behavior) |
| Portrait image | Static display, no interaction |

No animations in this phase (scroll-triggered reveals are v2 per INTE-01). No hover states beyond those already defined on Button and link components. No JavaScript interactions -- all sections are Server Components.

---

## Accessibility Contracts

| Requirement | Implementation |
|-------------|----------------|
| A11Y-04: Alt on all images | Portrait: `alt="Stephane Maire, consultant numerique"`. Benefice icons: `aria-hidden="true"` (decorative, meaning carried by text). |
| Semantic headings | H1 (Hero name) > H2 (section titles) > H3 (card titles, step titles, benefit titles). Single H1 on page. |
| Tap targets | All `<a>` elements (phone, email, social, CTAs): min 48px height via `min-h-12` or padding. |
| Link purpose | Phone link includes visible phone number. Email link includes visible email. Social links include `aria-label` with platform name. |
| Table accessibility | Formats table uses `<thead>`, `<th scope="col">`, `<tbody>`, `<tr>`, `<td>`. |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | not applicable | not applicable |
| Third-party | none | not applicable |

No external component registries. All components are hand-built project primitives.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
