---
phase: 04-seo-legal-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/app/icon.svg
  - src/app/apple-icon.png
  - src/app/favicon.ico
  - public/images/photo-portrait.webp
  - src/components/sections/HeroSection.tsx
autonomous: true
requirements: [SEO-01, SEO-02, SEO-03, SEO-04, SEO-05, PERF-05, POLI-02]

must_haves:
  truths:
    - "Google search preview shows title with Saone-et-Loire and relevant description with phone number"
    - "JSON-LD LocalBusiness structured data is present in the page HTML with name, address, telephone, email, sameAs"
    - "Favicon displays SM initials in brown/ecru palette in browser tab"
    - "Portrait image is served as WebP under 100KB"
    - "Heading hierarchy is H1 > H2 > H3 with exactly one H1"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Updated metadata + JSON-LD script tag"
      contains: "Saone-et-Loire"
    - path: "src/app/icon.svg"
      provides: "SVG favicon with SM initials"
      contains: "SM"
    - path: "src/app/apple-icon.png"
      provides: "Apple touch icon 180x180"
    - path: "src/app/favicon.ico"
      provides: "ICO favicon for legacy browsers"
    - path: "public/images/photo-portrait.webp"
      provides: "Optimized WebP portrait image"
  key_links:
    - from: "src/app/layout.tsx"
      to: "Google Rich Results"
      via: "JSON-LD script tag in body"
      pattern: "application/ld\\+json"
    - from: "src/app/layout.tsx"
      to: "Search engine results"
      via: "Metadata export"
      pattern: "Saone-et-Loire"
    - from: "src/components/sections/HeroSection.tsx"
      to: "public/images/photo-portrait.webp"
      via: "img src attribute"
      pattern: "photo-portrait.webp"
---

<objective>
Complete SEO metadata, JSON-LD structured data, favicon, and image optimization.

Purpose: Make the site discoverable by search engines with correct local business structured data, branded favicon, and optimized images for fast mobile loading.
Output: Updated layout.tsx with full SEO meta + JSON-LD, favicon files in src/app/, WebP portrait image.
</objective>

<execution_context>
@/Users/stephane/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stephane/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-seo-legal-polish/04-RESEARCH.md

<interfaces>
<!-- Existing layout.tsx metadata structure (to be updated, not replaced) -->
From src/app/layout.tsx:
```typescript
const siteTitle = "Stéphane Maire — Consultant numérique de proximité";
const siteDescription = "Consultant numérique indépendant pour artisans...";
const siteUrl = "https://un-bourguignon.com";

export const metadata: Metadata = {
  title: siteTitle,
  description: siteDescription,
  metadataBase: new URL(siteUrl),
  openGraph: { title, description, url, siteName, locale: "fr_FR", type: "website" },
  twitter: { card: "summary", title, description },
};
```

From src/components/sections/HeroSection.tsx:
```typescript
// Portrait image reference to update:
src="/images/photo-portrait.png"
```

From src/components/ui/Section.tsx:
```typescript
export function Section({ id, children, alternate, className }: SectionProps)
```

From src/components/ui/Container.tsx:
```typescript
export function Container({ children, className }: ContainerProps)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SEO metadata, Open Graph completion, and JSON-LD LocalBusiness</name>
  <files>src/app/layout.tsx</files>
  <read_first>
    - src/app/layout.tsx (current metadata state)
    - .planning/phases/04-seo-legal-polish/04-RESEARCH.md (JSON-LD object, metadata values)
    - src/components/Footer.tsx (LinkedIn/Facebook URLs for sameAs consistency)
  </read_first>
  <action>
Update `src/app/layout.tsx` with these exact changes:

1. **Update siteTitle** to:
```typescript
const siteTitle = "Stéphane Maire — Consultant numérique de proximité | Saône-et-Loire";
```

2. **Update siteDescription** to:
```typescript
const siteDescription = "Consultant numérique indépendant en Saône-et-Loire pour artisans, commerçants et TPE. Facturation électronique, outils de gestion, présence en ligne, formation IA. Contactez-moi au 06 62 46 16 43.";
```

3. **Keep the existing metadata export** as-is (it already references siteTitle/siteDescription/siteUrl, and already has OG locale fr_FR and type website). No structural changes needed to the metadata object.

4. **Add JSON-LD LocalBusiness script** inside the `<body>` tag, before `<Header />`:
```typescript
const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'LocalBusiness',
  name: 'Stéphane Maire',
  jobTitle: 'Consultant numérique de proximité',
  description: 'Consultant numérique indépendant pour artisans, commerçants et petits entrepreneurs en Saône-et-Loire et partout en France.',
  telephone: '+33662461643',
  email: 'stephane-ei@un-bourguignon.com',
  url: 'https://un-bourguignon.com',
  address: {
    '@type': 'PostalAddress',
    streetAddress: '2 rue des Lavoirs',
    addressLocality: 'Moroges',
    postalCode: '71390',
    addressRegion: 'Saône-et-Loire',
    addressCountry: 'FR',
  },
  areaServed: {
    '@type': 'Country',
    name: 'France',
  },
  sameAs: [
    'https://www.linkedin.com/in/stephanemaire71',
    'https://www.facebook.com/stephane.maire1',
  ],
  knowsAbout: [
    'Facturation électronique',
    'Outils numériques de gestion',
    'Présence en ligne locale',
    'Formation intelligence artificielle',
    "Création d'outils personnalisés",
  ],
};
```

Add inside `<body>`, as the first child before `<Header />`:
```tsx
<script
  type="application/ld+json"
  dangerouslySetInnerHTML={{
    __html: JSON.stringify(jsonLd).replace(/</g, '\\u003c'),
  }}
/>
```

5. **Do NOT change** the font loading, import structure, or component tree structure. The only changes are: siteTitle value, siteDescription value, and the JSON-LD script injection.
  </action>
  <verify>
    <automated>cd /Users/stephane/Work/un-bourguignon && npx next build 2>&1 | tail -5 && grep -c "Saône-et-Loire" out/index.html && grep -c "application/ld+json" out/index.html && grep -c "06 62 46 16 43" out/index.html</automated>
  </verify>
  <acceptance_criteria>
    - layout.tsx contains `const siteTitle = "Stéphane Maire — Consultant numérique de proximité | Saône-et-Loire"`
    - layout.tsx contains `const siteDescription` with "06 62 46 16 43" phone number
    - layout.tsx contains `type="application/ld+json"`
    - layout.tsx contains `'@type': 'LocalBusiness'`
    - layout.tsx contains `streetAddress: '2 rue des Lavoirs'`
    - layout.tsx contains `postalCode: '71390'`
    - layout.tsx contains `sameAs:` array with LinkedIn and Facebook URLs
    - layout.tsx contains `knowsAbout:` array with 5 service entries
    - out/index.html contains "Saône-et-Loire" (after build)
    - out/index.html contains "application/ld+json" (after build)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Meta title includes "Saône-et-Loire", description includes phone number, JSON-LD LocalBusiness with full address/contact/services is embedded in page HTML, OG tags are complete, build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Favicon SM initials + WebP portrait optimization</name>
  <files>src/app/icon.svg, src/app/apple-icon.png, src/app/favicon.ico, public/images/photo-portrait.webp, src/components/sections/HeroSection.tsx</files>
  <read_first>
    - src/components/sections/HeroSection.tsx (portrait img src to update)
    - public/images/photo-portrait.png (original portrait to convert)
    - .planning/phases/04-seo-legal-polish/04-RESEARCH.md (SVG template, favicon approach)
  </read_first>
  <action>
**Favicon creation (3 files in src/app/):**

1. **Create `src/app/icon.svg`** — SVG favicon with "SM" initials:
```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32">
  <rect width="32" height="32" rx="6" fill="#5B3E31"/>
  <text x="16" y="22" text-anchor="middle" font-family="Georgia, serif" font-size="16" font-weight="bold" fill="#F5F0E8">SM</text>
</svg>
```
Background: brun #5B3E31. Text: ecru #F5F0E8. Rounded square (rx=6).

2. **Generate `src/app/favicon.ico`** from the SVG using `sips` or ImageMagick. If neither produces .ico natively, use a 32x32 PNG renamed approach or create a minimal favicon.ico from the SVG. Command approach:
```bash
# Convert SVG to 32x32 PNG first, then to ICO
# If sips available (macOS):
sips -s format png --resampleWidth 32 --resampleHeight 32 icon.svg --out favicon-32.png
# Then use the PNG as favicon.ico (browsers accept PNG in .ico)
```
Alternatively, create a simple 32x32 PNG with the SM design and save as favicon.ico (modern browsers accept PNG format in .ico files).

3. **Generate `src/app/apple-icon.png`** — 180x180 PNG with same SM design:
```bash
# Render SVG at 180x180 as PNG
sips -s format png --resampleWidth 180 --resampleHeight 180 icon.svg --out apple-icon.png
```
If sips cannot handle SVG, use a programmatic approach: create a simple script or use `npx @vercel/og` to generate, OR create the PNG manually from the SVG using any available CLI tool.

**Portrait WebP conversion:**

4. **Convert `public/images/photo-portrait.png` to WebP:**
```bash
cd /Users/stephane/Work/un-bourguignon
sips -s format webp public/images/photo-portrait.png --out public/images/photo-portrait.webp
```
Target: under 100KB (original is 72KB PNG at 255x255, WebP should be ~20-40KB). Keep the original PNG in place.

5. **Update `src/components/sections/HeroSection.tsx`** — change the img src from:
```
src="/images/photo-portrait.png"
```
to:
```
src="/images/photo-portrait.webp"
```
Do NOT change any other part of HeroSection.tsx.

**Heading hierarchy audit (SEO-05):**

6. **Verify heading hierarchy** — the site should have exactly 1 `<h1>` (in HeroSection) and `<h2>` per section, `<h3>` for sub-items. This is already correct per Phase 3 implementation. Grep to confirm: `grep -rn "<h1\|<h2\|<h3" src/components/sections/`. No changes expected — this is verification only.
  </action>
  <verify>
    <automated>cd /Users/stephane/Work/un-bourguignon && test -f src/app/icon.svg && test -f src/app/favicon.ico && test -f src/app/apple-icon.png && test -f public/images/photo-portrait.webp && test $(stat -f%z public/images/photo-portrait.webp) -lt 102400 && grep -c "photo-portrait.webp" src/components/sections/HeroSection.tsx && grep -c "SM" src/app/icon.svg && echo "ALL CHECKS PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/icon.svg exists and contains `<text` with "SM" and fill="#F5F0E8" and rect fill="#5B3E31"
    - src/app/favicon.ico exists and is a valid image file (file size > 0)
    - src/app/apple-icon.png exists and is a valid PNG (file size > 0)
    - public/images/photo-portrait.webp exists and is under 102400 bytes
    - public/images/photo-portrait.png still exists (kept as original)
    - HeroSection.tsx contains `src="/images/photo-portrait.webp"` (not .png)
    - `grep -rn "<h1" src/components/sections/` returns exactly 1 result (HeroSection)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Favicon SM appears in browser tab, apple-touch-icon exists at 180px, portrait is served as WebP under 100KB, heading hierarchy is H1>H2>H3 with exactly one H1</done>
</task>

</tasks>

<verification>
After both tasks:
1. `npm run build` succeeds with zero errors
2. `out/index.html` contains "Saône-et-Loire" in title meta
3. `out/index.html` contains "application/ld+json" with LocalBusiness
4. `out/index.html` contains "photo-portrait.webp"
5. Favicon files exist in src/app/ (icon.svg, favicon.ico, apple-icon.png)
6. Portrait WebP is under 100KB
</verification>

<success_criteria>
- SEO meta title includes "| Saône-et-Loire"
- SEO description includes phone number "06 62 46 16 43"
- OG tags are complete (title, description, locale fr_FR, type website)
- JSON-LD LocalBusiness passes basic structure check (has @type, name, address, telephone, email, sameAs)
- Favicon SVG contains "SM" with brown bg / ecru text
- Portrait served as WebP, under 100KB
- Heading hierarchy correct (1 H1, multiple H2, H3 as needed)
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/04-seo-legal-polish/04-01-SUMMARY.md`
</output>
