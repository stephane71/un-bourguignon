---
phase: 03-content-sections
plan: 04
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - src/components/sections/ContactSection.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04
  - CONT-05

must_haves:
  truths:
    - "Contact section shows tappable phone tel: link and email mailto: link"
    - "Contact section has LinkedIn and Facebook links with icons"
    - "Phone and email blocks have minimum 48px tap target"
    - "page.tsx renders all 7 sections in narrative order from Hero to Contact"
    - "npm run build succeeds with the complete page"
  artifacts:
    - path: "src/components/sections/ContactSection.tsx"
      provides: "Contact section with phone, email, social links"
      exports: ["ContactSection"]
      min_lines: 30
    - path: "src/app/page.tsx"
      provides: "Home page composing all 7 sections"
      min_lines: 20
  key_links:
    - from: "src/components/sections/ContactSection.tsx"
      to: "src/components/icons/Phone.tsx"
      via: "import Phone icon"
      pattern: "import.*Phone.*from.*icons/Phone"
    - from: "src/components/sections/ContactSection.tsx"
      to: "src/components/icons/Mail.tsx"
      via: "import Mail icon"
      pattern: "import.*Mail.*from.*icons/Mail"
    - from: "src/components/sections/ContactSection.tsx"
      to: "src/components/icons/LinkedIn.tsx"
      via: "import existing LinkedIn icon"
      pattern: "import.*LinkedIn.*from.*icons/LinkedIn"
    - from: "src/components/sections/ContactSection.tsx"
      to: "src/components/icons/Facebook.tsx"
      via: "import existing Facebook icon"
      pattern: "import.*Facebook.*from.*icons/Facebook"
    - from: "src/app/page.tsx"
      to: "src/components/sections/HeroSection.tsx"
      via: "import and render"
      pattern: "import.*HeroSection.*from.*sections/HeroSection"
    - from: "src/app/page.tsx"
      to: "src/components/sections/ContactSection.tsx"
      via: "import and render"
      pattern: "import.*ContactSection.*from.*sections/ContactSection"
---

<objective>
Build the Contact section (final CTA with phone, email, social links) and wire all 7 sections into page.tsx -- replacing the design system demo with the real one-page narrative.

Purpose: Contact is the conversion endpoint. page.tsx wiring completes the phase -- the full narrative is live. Build verification confirms everything compiles.
Output: ContactSection.tsx + updated page.tsx with all 7 sections
</objective>

<execution_context>
@/Users/stephane/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stephane/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-content-sections/03-CONTEXT.md
@.planning/phases/03-content-sections/03-UI-SPEC.md
@content/presentation_activite.md
@.planning/phases/03-content-sections/03-01-SUMMARY.md
@.planning/phases/03-content-sections/03-02-SUMMARY.md
@.planning/phases/03-content-sections/03-03-SUMMARY.md

<interfaces>
From src/components/ui/Section.tsx:
```typescript
export function Section({ id, children, alternate = false, className = "" }: SectionProps)
```

From src/components/icons/ (existing):
```typescript
export function LinkedIn({ className = "", size = 24 }: LinkedInProps)  // fill-based
export function Facebook({ className = "", size = 24 }: FacebookProps)  // fill-based
```

From src/components/icons/ (created in Plan 01):
```typescript
export function Phone({ className = "", size = 24 }: PhoneProps)  // stroke-based
export function Mail({ className = "", size = 24 }: MailProps)    // stroke-based
```

From src/components/sections/ (created in Plans 01-03):
```typescript
export function HeroSection()       // id="hero"
export function AProposSection()    // id="a-propos"
export function PourQuiSection()    // id="pour-qui"
export function ServicesSection()   // id="services"
export function MethodeSection()    // id="methode"
export function BeneficesSection()  // id="benefices"
```

From src/app/layout.tsx:
```typescript
// layout.tsx already wraps {children} in <main>{children}</main>
// page.tsx must NOT add another <main> wrapper
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ContactSection component</name>
  <files>src/components/sections/ContactSection.tsx</files>
  <read_first>
    src/components/ui/Section.tsx
    src/components/icons/LinkedIn.tsx
    src/components/icons/Facebook.tsx
    src/components/icons/Phone.tsx
    src/components/icons/Mail.tsx
    content/presentation_activite.md
    .planning/phases/03-content-sections/03-UI-SPEC.md
  </read_first>
  <action>
Create `src/components/sections/ContactSection.tsx` as a Server Component (no `"use client"`).

Import Section from `@/components/ui/Section`, Phone from `@/components/icons/Phone`, Mail from `@/components/icons/Mail`, LinkedIn from `@/components/icons/LinkedIn`, Facebook from `@/components/icons/Facebook`.

Section uses `id="contact"` with NO `alternate` prop (white background -- 7th section).

Structure:
```
<Section id="contact">
  <div className="max-w-md mx-auto text-center">
    <h2 className="font-serif text-xl lg:text-4xl font-bold">
      Prêt à travailler ensemble ?
    </h2>
    <p className="mt-4 text-base leading-relaxed">
      Pour un premier échange sans engagement, contactez-moi directement :
    </p>

    <div className="mt-8 flex flex-col gap-4">
      {/* Phone block -- primary CTA style */}
      <a
        href="tel:+33662461643"
        className="flex items-center justify-center gap-3 min-h-12 w-full bg-brun text-white rounded-lg px-6 py-3 font-sans font-bold text-base hover:bg-brun/90 transition-colors focus:outline-none focus:ring-2 focus:ring-brun focus:ring-offset-2"
      >
        <Phone size={20} />
        06 62 46 16 43
      </a>

      {/* Email block -- secondary style */}
      <a
        href="mailto:stephane-ei@un-bourguignon.com"
        className="flex items-center justify-center gap-3 min-h-12 w-full border border-brun text-brun rounded-lg px-6 py-3 font-sans font-bold text-base hover:bg-ecru transition-colors focus:outline-none focus:ring-2 focus:ring-brun focus:ring-offset-2"
      >
        <Mail size={20} />
        stephane-ei@un-bourguignon.com
      </a>
    </div>

    {/* Social links */}
    <div className="mt-6 flex items-center justify-center gap-6">
      <a
        href="https://www.linkedin.com/in/stephane-maire/"
        aria-label="LinkedIn"
        className="min-h-12 min-w-12 flex items-center justify-center text-brun hover:text-brun/70 transition-colors focus:outline-none focus:ring-2 focus:ring-brun focus:ring-offset-2 rounded-lg"
      >
        <LinkedIn size={24} />
      </a>
      <a
        href="https://www.facebook.com/stephane.maire"
        aria-label="Facebook"
        className="min-h-12 min-w-12 flex items-center justify-center text-brun hover:text-brun/70 transition-colors focus:outline-none focus:ring-2 focus:ring-brun focus:ring-offset-2 rounded-lg"
      >
        <Facebook size={24} />
      </a>
    </div>
  </div>
</Section>
```

Key details:
- Phone link uses `tel:+33662461643` (international format) with visible "06 62 46 16 43" text
- Email link uses `mailto:stephane-ei@un-bourguignon.com`
- Phone block styled as primary (bg-brun text-white) -- most important action
- Email block styled as secondary (border-brun, text-brun) -- second option
- Both have min-h-12 (48px) for tap target per A11Y requirement
- Social links have `aria-label` for accessibility (icons are aria-hidden)
- Social links have min-h-12 min-w-12 (48px) tap target
- Content centered with max-w-md mx-auto
- Mobile: everything stacks naturally (flex-col)
- No form elements per CONT-05 (direct contact only)
- LinkedIn/Facebook URLs should use the actual URLs -- check the Footer component for the correct URLs to reuse. If not available, use placeholder URLs that the executor can verify against the Footer.

NOTE: The LinkedIn and Facebook URLs above are placeholders. The executor MUST read `src/components/Footer.tsx` to get the actual URLs used in the footer and use the same URLs here for consistency.
  </action>
  <verify>
    <automated>grep -q 'export function ContactSection' src/components/sections/ContactSection.tsx && grep -q 'id="contact"' src/components/sections/ContactSection.tsx && grep -q 'tel:+33662461643' src/components/sections/ContactSection.tsx && grep -q 'mailto:stephane-ei@un-bourguignon.com' src/components/sections/ContactSection.tsx && grep -q 'aria-label="LinkedIn"' src/components/sections/ContactSection.tsx && grep -q 'aria-label="Facebook"' src/components/sections/ContactSection.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - File exports `ContactSection` as a named export
    - Contains `<Section id="contact">` with NO `alternate` prop (white background)
    - Contains `<h2` with text "Prêt à travailler ensemble ?" (H2 level, with accent)
    - Contains `<a href="tel:+33662461643"` with visible text "06 62 46 16 43"
    - Contains `<a href="mailto:stephane-ei@un-bourguignon.com"` with visible email
    - Phone block has `min-h-12` and `bg-brun text-white` classes
    - Email block has `min-h-12` and `border border-brun text-brun` classes
    - Imports Phone and Mail from `@/components/icons/`
    - Imports LinkedIn and Facebook from `@/components/icons/`
    - Social links have `aria-label` attributes
    - Social links have `min-h-12 min-w-12` for 48px tap target
    - Contains `max-w-md mx-auto` for centered layout
    - Does NOT contain any `<form` or `<input` or `<textarea` elements
    - Does NOT contain `"use client"` directive
  </acceptance_criteria>
  <done>ContactSection renders phone tel: link (primary), email mailto: link (secondary), and LinkedIn/Facebook social links with proper tap targets and accessibility</done>
</task>

<task type="auto">
  <name>Task 2: Wire all 7 sections into page.tsx and verify build</name>
  <files>src/app/page.tsx</files>
  <read_first>
    src/app/page.tsx
    src/app/layout.tsx
    src/components/sections/HeroSection.tsx
    src/components/sections/AProposSection.tsx
    src/components/sections/PourQuiSection.tsx
    src/components/sections/ServicesSection.tsx
    src/components/sections/MethodeSection.tsx
    src/components/sections/BeneficesSection.tsx
    src/components/sections/ContactSection.tsx
  </read_first>
  <action>
Replace the entire content of `src/app/page.tsx` with:

```typescript
import { HeroSection } from "@/components/sections/HeroSection";
import { AProposSection } from "@/components/sections/AProposSection";
import { PourQuiSection } from "@/components/sections/PourQuiSection";
import { ServicesSection } from "@/components/sections/ServicesSection";
import { MethodeSection } from "@/components/sections/MethodeSection";
import { BeneficesSection } from "@/components/sections/BeneficesSection";
import { ContactSection } from "@/components/sections/ContactSection";

export default function Home() {
  return (
    <>
      <HeroSection />
      <AProposSection />
      <PourQuiSection />
      <ServicesSection />
      <MethodeSection />
      <BeneficesSection />
      <ContactSection />
    </>
  );
}
```

Key details:
- Uses a fragment `<>` -- NOT `<main>` (layout.tsx already wraps children in `<main>`)
- Removes ALL old design-system demo content (colors, typography, buttons, cards sections)
- Removes old imports (Button, Card, Section, Container are no longer directly used in page.tsx)
- Sections rendered in exact narrative order: Hero, À propos, Pour qui, Services, Méthode, Bénéfices, Contact
- No props passed -- all section components are self-contained with hardcoded content

After writing the file, run `npm run build` to verify the entire site compiles as a static export. Then run `npm run lint` to check for any linting issues.
  </action>
  <verify>
    <automated>cd /Users/stephane/Work/un-bourguignon && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx imports all 7 section components from `@/components/sections/`
    - page.tsx renders sections in order: HeroSection, AProposSection, PourQuiSection, ServicesSection, MethodeSection, BeneficesSection, ContactSection
    - page.tsx uses fragment `<>` wrapper, NOT `<main>`
    - page.tsx does NOT import Button, Card, Section, or Container directly
    - page.tsx does NOT contain the old design-system demo content (no "Systeme de design", no color swatches, no typography specimens)
    - `npm run build` exits with code 0 (static export succeeds)
    - `npm run lint` exits with code 0
  </acceptance_criteria>
  <done>page.tsx renders all 7 sections in narrative order, build succeeds as static export, old demo content removed</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0 (static export with all 7 sections)
- `npm run lint` exits 0
- `grep -c 'Section' src/app/page.tsx` returns 7 (7 section component references)
- Contact section has tel: and mailto: links
- No `<main>` in page.tsx (layout.tsx handles it)
</verification>

<success_criteria>
- Contact section has tappable phone (tel:) and email (mailto:) links with 48px tap targets
- Contact section shows LinkedIn and Facebook links with aria-labels
- page.tsx renders complete narrative from Hero through Contact
- Full site builds successfully as static export
- Design system demo content fully replaced
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-sections/03-04-SUMMARY.md`
</output>
