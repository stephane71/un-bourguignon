---
phase: 02-layout-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/icons/LinkedIn.tsx
  - src/components/icons/Facebook.tsx
  - src/components/icons/Burger.tsx
  - src/components/icons/Close.tsx
  - src/components/Footer.tsx
  - src/app/globals.css
autonomous: true
requirements:
  - FOOT-01
  - FOOT-02
  - NAV-04
  - A11Y-02

must_haves:
  truths:
    - "Footer displays LinkedIn and Facebook icon links, phone number, email, and mentions legales link"
    - "Phone link opens dialer with correct E.164 number (+33662461643)"
    - "Email link opens mail client to stephane-ei@un-bourguignon.com"
    - "All footer links have 48px minimum tap target"
    - "Smooth scroll is enabled in CSS and disabled when prefers-reduced-motion is set"
    - "Anchor scroll targets are offset by header height via scroll-margin-top"
  artifacts:
    - path: "src/components/icons/LinkedIn.tsx"
      provides: "LinkedIn inline SVG icon"
      contains: "aria-hidden"
    - path: "src/components/icons/Facebook.tsx"
      provides: "Facebook inline SVG icon"
      contains: "aria-hidden"
    - path: "src/components/icons/Burger.tsx"
      provides: "Hamburger menu inline SVG icon"
      contains: "aria-hidden"
    - path: "src/components/icons/Close.tsx"
      provides: "Close X inline SVG icon"
      contains: "aria-hidden"
    - path: "src/components/Footer.tsx"
      provides: "Footer component with social, contact, legal links"
      exports: ["Footer"]
    - path: "src/app/globals.css"
      provides: "Scroll behavior and scroll margin CSS rules"
      contains: "scroll-behavior"
  key_links:
    - from: "src/components/Footer.tsx"
      to: "src/components/icons/LinkedIn.tsx"
      via: "import"
      pattern: "import.*LinkedIn.*from"
    - from: "src/components/Footer.tsx"
      to: "src/components/icons/Facebook.tsx"
      via: "import"
      pattern: "import.*Facebook.*from"
---

<objective>
Create icon SVG components, the Footer component, and CSS scroll infrastructure for anchor navigation.

Purpose: Establish the reusable icon primitives needed by both Footer and Header, build the complete Footer with social/contact/legal links, and add CSS rules for smooth anchor scrolling with header offset.
Output: 4 icon components, Footer component, updated globals.css
</objective>

<execution_context>
@/Users/stephane/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stephane/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-layout-shell/02-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/ui/Container.tsx:
```typescript
interface ContainerProps {
  children: React.ReactNode;
  className?: string;
}
export function Container({ children, className = "" }: ContainerProps)
```

From src/app/globals.css (current state):
```css
@import "tailwindcss";

@theme {
  --color-ecru: #F5F0E8;
  --color-sable: #E8DCC8;
  --color-brun: #6B4C3B;
  --color-terre: #8B6914;
  --color-or: #C5973B;
  --color-or-light: #D4A574;
  --color-argile: #B8754C;
}

@theme inline {
  --font-serif: var(--font-lora), ui-serif, Georgia, serif;
  --font-sans: var(--font-instrument), ui-sans-serif, system-ui, sans-serif;
}

:root {
  color-scheme: light;
  --color-ecru: #F5F0E8;
  --color-sable: #E8DCC8;
  --color-brun: #6B4C3B;
  --color-terre: #8B6914;
  --color-or: #C5973B;
  --color-or-light: #D4A574;
  --color-argile: #B8754C;
}

body {
  color: var(--color-brun);
  background-color: #ffffff;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create icon SVG components and Footer</name>
  <read_first>
    - src/app/globals.css
    - src/components/ui/Container.tsx
    - src/components/ui/Button.tsx
    - .planning/phases/02-layout-shell/02-UI-SPEC.md
  </read_first>
  <files>
    src/components/icons/LinkedIn.tsx,
    src/components/icons/Facebook.tsx,
    src/components/icons/Burger.tsx,
    src/components/icons/Close.tsx,
    src/components/Footer.tsx
  </files>
  <action>
Create 4 icon components in `src/components/icons/`. Each icon component:
- Accepts `{ className?: string; size?: number }` props with defaults `className = ""` and `size = 24`
- Returns an `<svg>` element with `xmlns="http://www.w3.org/2000/svg"`, `viewBox="0 0 24 24"`, `fill="currentColor"`, `width={size}`, `height={size}`, `className={className}`, `aria-hidden="true"`
- Named export (not default export)

**LinkedIn.tsx** SVG path:
```
M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433a2.062 2.062 0 01-2.063-2.065 2.064 2.064 0 112.063 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z
```

**Facebook.tsx** SVG path:
```
M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z
```

**Burger.tsx** SVG: Three horizontal lines (hamburger icon). Use `stroke="currentColor"` with `strokeWidth={2}` and `strokeLinecap="round"` instead of fill. Three `<line>` elements: y=6, y=12, y=18 (all from x1=3 to x2=21). Set `fill="none"` on the svg.

**Close.tsx** SVG: X shape. Use `stroke="currentColor"` with `strokeWidth={2}` and `strokeLinecap="round"`. Two `<line>` elements: (6,6)-(18,18) and (6,18)-(18,6). Set `fill="none"` on the svg.

**Footer.tsx** — Server Component (no `"use client"`):
- Import `Container` from `@/components/ui/Container`
- Import `LinkedIn` from `@/components/icons/LinkedIn`
- Import `Facebook` from `@/components/icons/Facebook`
- Named export `Footer`
- Outer element: `<footer className="bg-brun text-white py-6">`
- Inside: `<Container className="flex flex-col sm:flex-row items-center justify-center gap-4 sm:gap-8 text-sm">`
- Content order (4 groups):
  1. Social icons div: `<div className="flex items-center gap-4">` containing LinkedIn and Facebook links
  2. Phone link
  3. Email link
  4. Mentions legales link

Social icon links use:
- LinkedIn: `href="https://linkedin.com/in/stephane-maire"` (placeholder URL), `aria-label="LinkedIn"`
- Facebook: `href="https://facebook.com/stephane.maire"` (placeholder URL), `aria-label="Facebook"`
- Both wrapped in: `className="min-h-12 min-w-12 inline-flex items-center justify-center hover:text-sable transition-colors focus:outline-none focus:ring-2 focus:ring-white focus:ring-offset-2 focus:ring-offset-brun"`

Phone link: `href="tel:+33662461643"`, display text `06 62 46 16 43`, className `"min-h-12 inline-flex items-center px-2 hover:text-sable transition-colors focus:outline-none focus:ring-2 focus:ring-white focus:ring-offset-2 focus:ring-offset-brun"`

Email link: `href="mailto:stephane-ei@un-bourguignon.com"`, display text `stephane-ei@un-bourguignon.com`, same className pattern as phone.

Mentions legales link: `href="/mentions-legales"`, display text `Mentions legales`, same className pattern as phone.
  </action>
  <verify>
    <automated>cd /Users/stephane/Work/un-bourguignon && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/icons/LinkedIn.tsx contains `export function LinkedIn`
    - src/components/icons/LinkedIn.tsx contains `aria-hidden="true"`
    - src/components/icons/Facebook.tsx contains `export function Facebook`
    - src/components/icons/Burger.tsx contains `export function Burger`
    - src/components/icons/Burger.tsx contains `stroke="currentColor"`
    - src/components/icons/Close.tsx contains `export function Close`
    - src/components/Footer.tsx contains `export function Footer`
    - src/components/Footer.tsx contains `bg-brun text-white`
    - src/components/Footer.tsx contains `tel:+33662461643`
    - src/components/Footer.tsx contains `mailto:stephane-ei@un-bourguignon.com`
    - src/components/Footer.tsx contains `/mentions-legales`
    - src/components/Footer.tsx contains `min-h-12 min-w-12`
    - src/components/Footer.tsx contains `aria-label="LinkedIn"`
    - src/components/Footer.tsx contains `aria-label="Facebook"`
    - src/components/Footer.tsx does NOT contain `"use client"`
    - npm run build succeeds
  </acceptance_criteria>
  <done>Four icon components render inline SVGs with aria-hidden. Footer displays social icons (LinkedIn, Facebook), phone (tel: link), email (mailto: link), and mentions legales link. All interactive elements have min-h-12 for 48px tap targets and visible focus rings. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add scroll infrastructure to globals.css</name>
  <read_first>
    - src/app/globals.css
    - .planning/phases/02-layout-shell/02-UI-SPEC.md
  </read_first>
  <files>src/app/globals.css</files>
  <action>
Append the following CSS rules to the end of `src/app/globals.css` (after the existing `body` block):

```css
html {
  scroll-behavior: smooth;
}

@media (prefers-reduced-motion: reduce) {
  html {
    scroll-behavior: auto;
  }
}

section[id] {
  scroll-margin-top: 5rem;
}
```

The `5rem` (80px) value accounts for the header height (64px mobile / 80px desktop) plus breathing room. Do NOT modify any existing CSS rules -- only append.
  </action>
  <verify>
    <automated>cd /Users/stephane/Work/un-bourguignon && grep -c "scroll-behavior: smooth" src/app/globals.css && grep -c "prefers-reduced-motion" src/app/globals.css && grep -c "scroll-margin-top" src/app/globals.css && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/app/globals.css contains `scroll-behavior: smooth`
    - src/app/globals.css contains `prefers-reduced-motion: reduce`
    - src/app/globals.css contains `scroll-behavior: auto` (inside reduced motion media query)
    - src/app/globals.css contains `scroll-margin-top: 5rem`
    - src/app/globals.css contains `section[id]`
    - All existing @theme, @theme inline, :root, and body blocks are unchanged
    - npm run build succeeds
  </acceptance_criteria>
  <done>globals.css has smooth scroll enabled, reduced-motion fallback to auto, and section[id] scroll-margin-top of 5rem for header offset. Build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- `grep -r "aria-hidden" src/components/icons/` returns matches for all 4 icon files
- `grep "min-h-12" src/components/Footer.tsx` confirms tap target sizing on all links
- `grep "scroll-behavior" src/app/globals.css` shows both smooth and auto rules
- `grep "scroll-margin-top" src/app/globals.css` shows 5rem offset
</verification>

<success_criteria>
- Four inline SVG icon components exist and export named functions
- Footer renders with social links, phone, email, mentions legales -- all with 48px tap targets and focus rings
- globals.css contains smooth scroll, reduced-motion override, and scroll-margin-top for section anchors
- `npm run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-layout-shell/02-01-SUMMARY.md`
</output>
