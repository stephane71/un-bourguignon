---
phase: 03-content-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/icons/Phone.tsx
  - src/components/icons/Mail.tsx
  - src/components/icons/Clock.tsx
  - src/components/icons/Key.tsx
  - src/components/icons/UserCheck.tsx
  - src/components/icons/CoinStack.tsx
  - src/components/sections/HeroSection.tsx
  - src/components/sections/AProposSection.tsx
autonomous: true
requirements:
  - HERO-01
  - HERO-02
  - HERO-03
  - HERO-04
  - HERO-05
  - HERO-06
  - APRO-01
  - APRO-02
  - A11Y-04

must_haves:
  truths:
    - "6 new SVG icon components exist with consistent API (className, size, aria-hidden)"
    - "Hero shows H1 name, subtitle, slogan italic, circular portrait, two CTAs"
    - "Hero layout is portrait-above-text on mobile, side-by-side on desktop"
    - "A propos section shows prose content with key points emphasized"
  artifacts:
    - path: "src/components/icons/Phone.tsx"
      provides: "Phone handset SVG icon"
      exports: ["Phone"]
    - path: "src/components/icons/Mail.tsx"
      provides: "Mail envelope SVG icon"
      exports: ["Mail"]
    - path: "src/components/icons/Clock.tsx"
      provides: "Clock SVG icon for time-saved benefit"
      exports: ["Clock"]
    - path: "src/components/icons/Key.tsx"
      provides: "Key SVG icon for autonomy benefit"
      exports: ["Key"]
    - path: "src/components/icons/UserCheck.tsx"
      provides: "Person+checkmark SVG icon for single-contact benefit"
      exports: ["UserCheck"]
    - path: "src/components/icons/CoinStack.tsx"
      provides: "Coin SVG icon for fair-pricing benefit"
      exports: ["CoinStack"]
    - path: "src/components/sections/HeroSection.tsx"
      provides: "Hero section with full content"
      exports: ["HeroSection"]
      min_lines: 40
    - path: "src/components/sections/AProposSection.tsx"
      provides: "A propos section with prose content"
      exports: ["AProposSection"]
      min_lines: 20
  key_links:
    - from: "src/components/sections/HeroSection.tsx"
      to: "src/components/ui/Button.tsx"
      via: "import Button, render as anchor"
      pattern: "Button.*as=.a.*href=.#contact"
    - from: "src/components/sections/HeroSection.tsx"
      to: "src/components/ui/Section.tsx"
      via: "import Section wrapper"
      pattern: "Section.*id=.hero"
---

<objective>
Create all 6 new SVG icon components and build the Hero and A propos sections -- the first two narrative sections of the page.

Purpose: Icons are needed by later sections (Benefices, Contact). Hero is the landing area and most complex layout. A propos is straightforward prose. Completing these first unblocks all downstream plans.
Output: 6 icon files in src/components/icons/, 2 section components in src/components/sections/
</objective>

<execution_context>
@/Users/stephane/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stephane/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-content-sections/03-CONTEXT.md
@.planning/phases/03-content-sections/03-UI-SPEC.md
@content/presentation_activite.md

<interfaces>
<!-- Existing component APIs the executor must use -->

From src/components/ui/Section.tsx:
```typescript
interface SectionProps {
  id: string;
  children: React.ReactNode;
  alternate?: boolean;
  className?: string;
}
export function Section({ id, children, alternate = false, className = "" }: SectionProps)
// Renders: <section id={id} className={`${alternate ? "bg-ecru" : "bg-white"} py-16 sm:py-20 lg:py-24 ${className}`}>
```

From src/components/ui/Button.tsx:
```typescript
export function Button({ variant = "primary", children, className = "", ...props }: ButtonProps)
// Usage as anchor: <Button as="a" href="#contact" variant="primary">Prendre contact</Button>
```

From src/components/ui/Card.tsx:
```typescript
export function Card({ children, variant = "on-white", className = "" }: CardProps)
// Variants: "on-white" (bg-white border border-sable), "on-ecru" (bg-white)
```

From src/components/icons/LinkedIn.tsx (pattern to follow for all new icons):
```typescript
interface LinkedInProps {
  className?: string;
  size?: number;
}
export function LinkedIn({ className = "", size = 24 }: LinkedInProps) {
  return (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"
      fill="currentColor" width={size} height={size}
      className={className} aria-hidden="true">
      <path ... />
    </svg>
  );
}
```
Note: New icons use stroke-based style (fill="none", stroke="currentColor", strokeWidth={1.5}) instead of fill-based. Social icons (LinkedIn, Facebook) keep fill="currentColor".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 6 new SVG icon components</name>
  <files>
    src/components/icons/Phone.tsx
    src/components/icons/Mail.tsx
    src/components/icons/Clock.tsx
    src/components/icons/Key.tsx
    src/components/icons/UserCheck.tsx
    src/components/icons/CoinStack.tsx
  </files>
  <read_first>
    src/components/icons/LinkedIn.tsx
    src/components/icons/Facebook.tsx
  </read_first>
  <action>
Create 6 inline SVG icon components following the exact pattern from LinkedIn.tsx but using stroke-based thin-line style instead of fill-based.

Each icon file follows this template:
```typescript
interface {Name}Props {
  className?: string;
  size?: number;
}

export function {Name}({ className = "", size = 24 }: {Name}Props) {
  return (
    <svg
      xmlns="http://www.w3.org/2000/svg"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      strokeWidth={1.5}
      strokeLinecap="round"
      strokeLinejoin="round"
      width={size}
      height={size}
      className={className}
      aria-hidden="true"
    >
      {/* SVG path(s) */}
    </svg>
  );
}
```

Icon SVG paths (all use 24x24 viewBox, stroke-based):

**Phone.tsx** -- handset outline:
`<path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z" />`

**Mail.tsx** -- envelope outline:
`<rect width="20" height="16" x="2" y="4" rx="2" /><path d="m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7" />`

**Clock.tsx** -- clock face outline (for "time saved" benefit):
`<circle cx="12" cy="12" r="10" /><polyline points="12 6 12 12 16 14" />`

**Key.tsx** -- key outline (for "autonomy" benefit):
`<path d="m21 2-2 2m-7.61 7.61a5.5 5.5 0 1 1-7.778 7.778 5.5 5.5 0 0 1 7.777-7.777zm0 0L15.5 7.5m0 0 3 3L22 7l-3-3m-3.5 3.5L19 4" />`

**UserCheck.tsx** -- person with checkmark (for "single contact" benefit):
`<path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2" /><circle cx="9" cy="7" r="4" /><polyline points="16 11 18 13 22 9" />`

**CoinStack.tsx** -- coin/euro outline (for "fair pricing" benefit):
`<circle cx="12" cy="12" r="10" /><path d="M15 9.354a4 4 0 1 0 0 5.292" /><line x1="9" y1="12" x2="15" y2="12" />`

The CoinStack icon renders as a euro symbol inside a circle -- simple and recognizable for "fair pricing".
  </action>
  <verify>
    <automated>ls src/components/icons/{Phone,Mail,Clock,Key,UserCheck,CoinStack}.tsx && echo "All 6 icons exist"</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 6 files exports a named function component matching the filename (e.g., `export function Phone`)
    - Each component accepts `className?: string` and `size?: number` props with defaults `""` and `24`
    - Each SVG has `aria-hidden="true"`, `fill="none"`, `stroke="currentColor"`, `strokeWidth={1.5}`
    - Each SVG has `viewBox="0 0 24 24"`, `strokeLinecap="round"`, `strokeLinejoin="round"`
    - No `"use client"` directive in any icon file
  </acceptance_criteria>
  <done>6 icon components created with consistent stroke-based API matching existing icon pattern</done>
</task>

<task type="auto">
  <name>Task 2: Create HeroSection component</name>
  <files>src/components/sections/HeroSection.tsx</files>
  <read_first>
    src/components/ui/Section.tsx
    src/components/ui/Button.tsx
    src/app/layout.tsx
    content/presentation_activite.md
    .planning/phases/03-content-sections/03-UI-SPEC.md
  </read_first>
  <action>
Create `src/components/sections/HeroSection.tsx` as a Server Component (no `"use client"`).

Import Section from `@/components/ui/Section`, Button from `@/components/ui/Button`, Image from `next/image`.

Structure:
```
<Section id="hero" className="!py-20 sm:!py-28 lg:!py-32">
  <div className="flex flex-col items-center lg:flex-row lg:items-center lg:justify-between gap-8 lg:gap-12">
    {/* Portrait -- appears first on mobile (natural order), on the right on desktop */}
    <div className="lg:order-last flex-shrink-0">
      <Image
        src="/images/photo-portrait.png"
        alt="Stéphane Maire, consultant numérique"
        width={280}
        height={280}
        className="rounded-full w-[200px] h-[200px] lg:w-[280px] lg:h-[280px]"
        priority
      />
    </div>
    {/* Text block */}
    <div className="text-center lg:text-left">
      <h1 className="font-serif text-4xl lg:text-5xl font-bold">Stéphane Maire</h1>
      <p className="font-sans text-xl lg:text-2xl mt-3" style={{ lineHeight: "1.4" }}>
        Consultant numérique de proximité
      </p>
      <p className="font-serif text-xl italic mt-4" style={{ lineHeight: "1.5" }}>
        La méthode d&apos;un expert, la proximité d&apos;un artisan.
      </p>
      <div className="flex flex-col sm:flex-row items-center lg:items-start gap-4 mt-8">
        <Button as="a" href="#contact">Prendre contact</Button>
        <a
          href="#services"
          className="font-sans font-bold text-brun underline-offset-4 hover:underline inline-flex items-center gap-1 min-h-12"
        >
          Découvrir mes services &rarr;
        </a>
      </div>
    </div>
  </div>
</Section>
```

Key details:
- Hero has `alternate` omitted (defaults to false = white background)
- Extra padding via `!py-20 sm:!py-28 lg:!py-32` using Tailwind important modifier to override Section's base padding
- Portrait uses `next/image` with `priority` for LCP optimization
- Portrait is 200px on mobile, 280px on desktop via Tailwind arbitrary values
- "Prendre contact" uses Button component as anchor to #contact (per HERO-04)
- "Découvrir mes services" is a plain text link with arrow, NOT a Button (per CONTEXT.md locked decision)
- All French accented characters preserved: Stéphane, numérique, proximité, méthode, Découvrir
- The `<main>` wrapper is already in layout.tsx -- do NOT add another one
- H1 is the only H1 on the page (semantic heading hierarchy)
  </action>
  <verify>
    <automated>grep -q 'export function HeroSection' src/components/sections/HeroSection.tsx && grep -q 'id="hero"' src/components/sections/HeroSection.tsx && grep -q 'Stéphane Maire' src/components/sections/HeroSection.tsx && grep -q 'priority' src/components/sections/HeroSection.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - File exports `HeroSection` as a named export
    - Contains `<Section id="hero"` with className including `!py-20`
    - Contains `<h1` with "Stéphane Maire" (with accent on e)
    - Contains subtitle "Consultant numérique de proximité" (with accents)
    - Contains slogan with `italic` class and text "La méthode d'un expert"
    - Contains `<Image` with `src="/images/photo-portrait.png"` and `priority` attribute
    - Contains `alt="Stéphane Maire, consultant numérique"` on the Image
    - Contains `<Button as="a" href="#contact">Prendre contact</Button>`
    - Contains `<a href="#services"` with text "Découvrir mes services" -- NOT wrapped in Button component
    - Contains `lg:order-last` on portrait container (desktop: portrait right)
    - Contains `rounded-full` on Image className
    - Contains `w-[200px] h-[200px] lg:w-[280px] lg:h-[280px]` on Image className
    - Does NOT contain `"use client"` directive
    - Does NOT contain `<main` tag
  </acceptance_criteria>
  <done>HeroSection renders H1 name, subtitle, italic slogan, circular portrait (200/280px), primary CTA button to #contact, secondary text link to #services, with mobile-first responsive layout</done>
</task>

<task type="auto">
  <name>Task 3: Create AProposSection component</name>
  <files>src/components/sections/AProposSection.tsx</files>
  <read_first>
    src/components/ui/Section.tsx
    content/presentation_activite.md
    .planning/phases/03-content-sections/03-UI-SPEC.md
  </read_first>
  <action>
Create `src/components/sections/AProposSection.tsx` as a Server Component.

Import Section from `@/components/ui/Section`.

Structure:
```
<Section id="a-propos" alternate>
  <h2 className="font-serif text-xl lg:text-4xl font-bold">Qui suis-je ?</h2>
  <div className="mt-8 max-w-3xl space-y-6">
    <p className="text-base leading-relaxed">
      Je suis Stéphane Maire, consultant numérique indépendant. Mon activité est née d'un constat
      simple : les petits entrepreneurs locaux — artisans, maraîchers, fleuristes, commerçants
      des marchés — sont souvent seuls face aux obligations numériques croissantes et aux outils
      qui évoluent sans cesse.
    </p>
    <p className="text-base leading-relaxed">
      J'ai choisi d'exercer en indépendant pour garder la liberté d'une relation directe, sans
      intermédiaire, avec chacun de mes clients. Pas de grand cabinet, pas de prestataire
      lointain : je me déplace, j'écoute, je m'adapte à la réalité concrète de chaque activité.
    </p>
    <p className="text-base leading-relaxed font-serif italic">
      La méthode d'un expert, la proximité d'un artisan.
    </p>
    <p className="text-base leading-relaxed">
      Je travaille avec la rigueur et les outils d'un consultant expérimenté, mais je reste
      accessible, disponible, et profondément ancré dans le tissu économique local.
    </p>
    <dl className="mt-8 grid grid-cols-1 sm:grid-cols-2 gap-4">
      <div>
        <dt className="font-serif font-bold text-xl">Expertise</dt>
        <dd className="mt-1 text-base leading-relaxed">Conseil numérique, facturation électronique, outils de gestion</dd>
      </div>
      <div>
        <dt className="font-serif font-bold text-xl">Proximité</dt>
        <dd className="mt-1 text-base leading-relaxed">Déplacement chez le client, accompagnement personnalisé</dd>
      </div>
      <div>
        <dt className="font-serif font-bold text-xl">À distance</dt>
        <dd className="mt-1 text-base leading-relaxed">Formation, conseil et création d'outils sur mesure</dd>
      </div>
      <div>
        <dt className="font-serif font-bold text-xl">Forme juridique</dt>
        <dd className="mt-1 text-base leading-relaxed">Entreprise individuelle — relation directe, sans sous-traitance</dd>
      </div>
    </dl>
  </div>
</Section>
```

Content transcribed verbatim from `content/presentation_activite.md` section 1 ("Qui suis-je ?"). Key points (indépendant, relation directe, déplacement, EI) are emphasized via the `<dl>` definition list at the bottom -- per APRO-02 and CONTEXT.md decision. All French accented characters must be preserved exactly as in the content source.
  </action>
  <verify>
    <automated>grep -q 'export function AProposSection' src/components/sections/AProposSection.tsx && grep -q 'id="a-propos"' src/components/sections/AProposSection.tsx && grep -q 'alternate' src/components/sections/AProposSection.tsx && grep -q 'Qui suis-je' src/components/sections/AProposSection.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - File exports `AProposSection` as a named export
    - Contains `<Section id="a-propos" alternate>`
    - Contains `<h2` with text "Qui suis-je ?" (H2, not H1)
    - Contains `max-w-3xl` for comfortable reading width
    - Contains `<dl` element with at least 4 `<dt>` / `<dd>` pairs for key points
    - Key point dt elements include "Expertise", "Proximité", "À distance", "Forme juridique"
    - Contains "consultant numérique indépendant" (accented) in prose
    - Does NOT contain `"use client"` directive
  </acceptance_criteria>
  <done>AProposSection renders section 1 content with H2 title, prose paragraphs, and key points as definition list on ecru background</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- All 6 icon files exist in src/components/icons/ with correct exports
- HeroSection.tsx and AProposSection.tsx exist in src/components/sections/
- `npm run lint` passes (eslint jsx-a11y checks alt attributes)
</verification>

<success_criteria>
- 6 stroke-based SVG icon components created with consistent API
- HeroSection shows name (H1), subtitle, italic slogan, circular portrait (200/280px), primary CTA button, secondary text link
- AProposSection shows prose content with key points in definition list on ecru background
- All content uses proper French accented characters
- No "use client" directives -- all Server Components
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-sections/03-01-SUMMARY.md`
</output>
