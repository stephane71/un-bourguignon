---
phase: 04-seo-legal-polish
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified: []
autonomous: false
requirements: [PERF-01, PERF-02, PERF-03, PERF-04]

must_haves:
  truths:
    - "Lighthouse Performance score is >= 90 on mobile"
    - "Lighthouse Accessibility score is >= 90 on mobile"
    - "Lighthouse SEO score is >= 95 on mobile"
    - "LCP on mobile 4G simulation is under 2.5 seconds"
  artifacts: []
  key_links: []
---

<objective>
Build verification and Lighthouse audit to confirm all SEO, accessibility, and performance targets are met.

Purpose: Validate that the combined work from plans 01 and 02 meets the quantitative Lighthouse targets (Performance >= 90, Accessibility >= 90, SEO >= 95) and qualitative checks (JSON-LD validity, print output, heading hierarchy).
Output: Verified build with documented Lighthouse scores.
</objective>

<execution_context>
@/Users/stephane/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stephane/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-seo-legal-polish/04-RESEARCH.md
@.planning/phases/04-seo-legal-polish/04-01-SUMMARY.md
@.planning/phases/04-seo-legal-polish/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build output verification and smoke tests</name>
  <files></files>
  <read_first>
    - .planning/phases/04-seo-legal-polish/04-01-SUMMARY.md (what was built in plan 01)
    - .planning/phases/04-seo-legal-polish/04-02-SUMMARY.md (what was built in plan 02)
    - .planning/phases/04-seo-legal-polish/04-RESEARCH.md (validation test map)
  </read_first>
  <action>
Run a full build and execute all smoke tests against the `out/` directory. This task modifies NO files — it only validates.

**Build:**
```bash
cd /Users/stephane/Work/un-bourguignon && npm run build
```

**Smoke test checklist (run each as a shell command):**

1. SEO-01 (title): `grep "Saône-et-Loire" out/index.html`
2. SEO-02 (description + phone): `grep "06 62 46 16 43" out/index.html`
3. SEO-03 (OG tags): `grep "og:title" out/index.html && grep "og:description" out/index.html && grep "og:locale" out/index.html`
4. SEO-04 (JSON-LD): `grep "application/ld+json" out/index.html && grep "LocalBusiness" out/index.html`
5. SEO-05 (heading hierarchy): `grep -c "<h1" out/index.html` should return 1
6. SEO-06 (mentions legales): `test -f out/mentions-legales/index.html`
7. PERF-05 (WebP portrait): `test -f out/images/photo-portrait.webp && test $(stat -f%z out/images/photo-portrait.webp) -lt 102400`
8. POLI-02 (favicon): `test -f out/favicon.ico && test -f out/icon.svg`
9. A11Y-01 (contrast fix): `grep "text-argile" src/components/sections/MethodeSection.tsx`
10. A11Y-05 (reduced-motion): `grep "transition-duration: 0.01ms" src/app/globals.css`
11. POLI-01 (print): `grep "@media print" src/app/globals.css`

If any check fails, diagnose and fix the issue before proceeding. Document all results.

**Lint check:**
```bash
npm run lint
```
  </action>
  <verify>
    <automated>cd /Users/stephane/Work/un-bourguignon && npm run build 2>&1 | tail -3 && echo "---SMOKE---" && grep -c "Saône-et-Loire" out/index.html && grep -c "06 62 46 16 43" out/index.html && grep -c "og:title" out/index.html && grep -c "application/ld+json" out/index.html && grep -c "<h1" out/index.html && test -f out/mentions-legales/index.html && echo "mentions-ok" && test -f out/favicon.ico && echo "favicon-ok" && echo "ALL SMOKE TESTS PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 with no errors
    - `npm run lint` exits 0 with no errors
    - out/index.html contains "Saône-et-Loire"
    - out/index.html contains "06 62 46 16 43"
    - out/index.html contains "og:title"
    - out/index.html contains "application/ld+json"
    - out/index.html has exactly 1 `<h1` tag
    - out/mentions-legales/index.html exists
    - out/favicon.ico exists
    - out/icon.svg exists
    - out/images/photo-portrait.webp exists and is under 102400 bytes
  </acceptance_criteria>
  <done>Full build succeeds, all 11 smoke tests pass, lint is clean</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Lighthouse audit and visual verification</name>
  <files></files>
  <action>
Present verification instructions to the user. Start local server with `npx serve out -l 3000` and guide user through Lighthouse audit and visual checks.

What was built across plans 01 and 02:
- Updated meta title/description with Saone-et-Loire localization
- JSON-LD LocalBusiness structured data
- /mentions-legales page with full legal content
- SM favicon in brown/ecru palette
- WebP portrait optimization
- Print stylesheet (hides nav/footer, shows content)
- Contrast fix (step numbers use argile instead of or-light)
- Extended prefers-reduced-motion support

Verification steps for user:
1. Start local server: `npx serve out -l 3000`
2. Lighthouse audit (mobile): Open http://localhost:3000 in Chrome, DevTools > Lighthouse tab, select Mobile, run. Targets: Performance >= 90, Accessibility >= 90, SEO >= 95
3. Favicon: Check browser tab shows "SM" icon
4. Mentions legales: Visit http://localhost:3000/mentions-legales -- verify SIRET, address, host
5. Print preview: Cmd+P on home page -- verify header/footer hidden, contact visible, no backgrounds
6. JSON-LD: Visit https://search.google.com/test/rich-results with page source
7. Reduced motion: Chrome DevTools > Rendering > Emulate prefers-reduced-motion: reduce -- no smooth scroll
  </action>
  <verify>User confirms "approved" or describes issues</verify>
  <done>Lighthouse scores meet targets (Performance >= 90, Accessibility >= 90, SEO >= 95), favicon visible, mentions legales correct, print output clean, JSON-LD valid</done>
</task>

</tasks>

<verification>
Phase 4 is complete when:
1. All automated smoke tests pass against build output
2. Lighthouse mobile scores meet targets (Performance >= 90, Accessibility >= 90, SEO >= 95)
3. User has visually verified favicon, mentions legales, print output, and JSON-LD
</verification>

<success_criteria>
- Lighthouse Performance >= 90 (mobile)
- Lighthouse Accessibility >= 90 (mobile)
- Lighthouse SEO >= 95 (mobile)
- LCP < 2.5s on mobile 4G simulation
- All smoke tests pass
- User approves visual/functional verification
</success_criteria>

<output>
After completion, create `.planning/phases/04-seo-legal-polish/04-03-SUMMARY.md`
</output>
