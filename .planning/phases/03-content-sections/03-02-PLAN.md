---
phase: 03-content-sections
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/PourQuiSection.tsx
  - src/components/sections/ServicesSection.tsx
autonomous: true
requirements:
  - POUR-01
  - POUR-02
  - POUR-03
  - SERV-01
  - SERV-02
  - SERV-03
  - SERV-04

must_haves:
  truths:
    - "Pour qui shows 4 problem cards stacked on mobile, 2x2 grid on desktop"
    - "Services shows 5 cards with gold top border in 1/2/3 column responsive grid"
    - "Each service card has title, description, and bullet list of sub-prestations"
    - "Cards 4 and 5 in Services are left-aligned at 3-column breakpoint"
  artifacts:
    - path: "src/components/sections/PourQuiSection.tsx"
      provides: "Pour qui section with 4 problem cards"
      exports: ["PourQuiSection"]
      min_lines: 30
    - path: "src/components/sections/ServicesSection.tsx"
      provides: "Services section with 5 domain cards"
      exports: ["ServicesSection"]
      min_lines: 60
  key_links:
    - from: "src/components/sections/PourQuiSection.tsx"
      to: "src/components/ui/Card.tsx"
      via: "import Card on-white variant"
      pattern: "Card.*variant=.on-white"
    - from: "src/components/sections/ServicesSection.tsx"
      to: "src/components/ui/Card.tsx"
      via: "import Card on-ecru variant with top border"
      pattern: "Card.*variant=.on-ecru"
---

<objective>
Build the Pour qui (problem cards) and Services (domain cards) sections -- the middle narrative arc explaining who Stephane helps and what he offers.

Purpose: These two card-based sections form the core value proposition. Pour qui uses plain text cards; Services uses accent-bordered cards with bullet lists.
Output: 2 section components in src/components/sections/
</objective>

<execution_context>
@/Users/stephane/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stephane/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-content-sections/03-CONTEXT.md
@.planning/phases/03-content-sections/03-UI-SPEC.md
@content/presentation_activite.md

<interfaces>
From src/components/ui/Section.tsx:
```typescript
interface SectionProps {
  id: string;
  children: React.ReactNode;
  alternate?: boolean;
  className?: string;
}
export function Section({ id, children, alternate = false, className = "" }: SectionProps)
```

From src/components/ui/Card.tsx:
```typescript
interface CardProps {
  children: React.ReactNode;
  variant?: "on-white" | "on-ecru";
  className?: string;
}
export function Card({ children, variant = "on-white", className = "" }: CardProps)
// "on-white": "bg-white border border-sable"
// "on-ecru": "bg-white" (no border by default)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PourQuiSection component</name>
  <files>src/components/sections/PourQuiSection.tsx</files>
  <read_first>
    src/components/ui/Section.tsx
    src/components/ui/Card.tsx
    content/presentation_activite.md
    .planning/phases/03-content-sections/03-UI-SPEC.md
  </read_first>
  <action>
Create `src/components/sections/PourQuiSection.tsx` as a Server Component (no `"use client"`).

Import Section from `@/components/ui/Section`, Card from `@/components/ui/Card`.

Section uses `id="pour-qui"` with NO `alternate` prop (white background -- 3rd section in alternation pattern).

Structure:
```
<Section id="pour-qui">
  <h2 className="font-serif text-xl lg:text-4xl font-bold">Pour qui ?</h2>
  <p className="mt-4 text-base leading-relaxed max-w-3xl">
    J'accompagne les micro-entrepreneurs et les entreprises individuelles qui gèrent seuls
    leur activité : le métier, les clients, et tout l'administratif. Qu'ils soient artisans,
    commerçants de marché, prestataires de services ou en cours de reconversion, ils partagent
    les mêmes défis face au numérique.
  </p>
  <div className="mt-8 grid grid-cols-1 md:grid-cols-2 gap-6">
    {/* Card 1 */}
    <Card variant="on-white">
      <h3 className="font-serif text-xl font-bold">Des outils qui s'imposent sans explication</h3>
      <p className="mt-2 text-base leading-relaxed">
        Logiciels de caisse, plateformes de déclaration, messageries professionnelles…
        Les outils numériques se multiplient sans que personne n'explique comment les
        choisir ni comment les utiliser efficacement.
      </p>
    </Card>
    {/* Card 2 */}
    <Card variant="on-white">
      <h3 className="font-serif text-xl font-bold">Des obligations administratives qui alourdissent le quotidien</h3>
      <p className="mt-2 text-base leading-relaxed">
        Facturation, suivi des devis, déclarations — autant de tâches chronophages qui
        empiètent sur le temps du métier et créent un stress permanent.
      </p>
    </Card>
    {/* Card 3 */}
    <Card variant="on-white">
      <h3 className="font-serif text-xl font-bold">Une présence en ligne difficile à construire seul</h3>
      <p className="mt-2 text-base leading-relaxed">
        Savoir qu'il faut être visible sur Google ou les réseaux sociaux, c'est une chose.
        Savoir comment faire, avec quel outil, et en combien de temps — c'en est une autre.
      </p>
    </Card>
    {/* Card 4 */}
    <Card variant="on-white">
      <h3 className="font-serif text-xl font-bold">Pas d'interlocuteur de confiance</h3>
      <p className="mt-2 text-base leading-relaxed">
        Les grandes agences sont trop chères, les amis qui s'y connaissent pas toujours
        disponibles, et les tutoriels en ligne rarement adaptés à une activité de terrain.
      </p>
    </Card>
  </div>
</Section>
```

Content transcribed verbatim from `content/presentation_activite.md` section 2 ("Pour qui ?"). Cards are text-only per locked decision (no icons on problem cards). Card variant is `on-white` with sable border (white card on white section background). All French accented characters preserved.
  </action>
  <verify>
    <automated>grep -q 'export function PourQuiSection' src/components/sections/PourQuiSection.tsx && grep -q 'id="pour-qui"' src/components/sections/PourQuiSection.tsx && grep -c 'Card variant="on-white"' src/components/sections/PourQuiSection.tsx | grep -q '4' && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - File exports `PourQuiSection` as a named export
    - Contains `<Section id="pour-qui">` with NO `alternate` prop (white background)
    - Contains `<h2` with text "Pour qui ?" (H2 level)
    - Contains exactly 4 `<Card variant="on-white">` elements
    - Each Card contains an `<h3` title and a `<p` description
    - Card titles match content source: "Des outils qui s'imposent sans explication", "Des obligations administratives qui alourdissent le quotidien", "Une présence en ligne difficile à construire seul", "Pas d'interlocuteur de confiance"
    - Grid uses `grid grid-cols-1 md:grid-cols-2 gap-6`
    - Does NOT contain any icon imports or icon components
    - Does NOT contain `"use client"` directive
  </acceptance_criteria>
  <done>PourQuiSection renders 4 text-only problem cards in a responsive 1-col/2-col grid on white background</done>
</task>

<task type="auto">
  <name>Task 2: Create ServicesSection component</name>
  <files>src/components/sections/ServicesSection.tsx</files>
  <read_first>
    src/components/ui/Section.tsx
    src/components/ui/Card.tsx
    content/presentation_activite.md
    .planning/phases/03-content-sections/03-UI-SPEC.md
  </read_first>
  <action>
Create `src/components/sections/ServicesSection.tsx` as a Server Component (no `"use client"`).

Import Section from `@/components/ui/Section`, Card from `@/components/ui/Card`.

Section uses `id="services"` with `alternate` prop (ecru background -- 4th section).

Structure:
```
<Section id="services" alternate>
  <h2 className="font-serif text-xl lg:text-4xl font-bold">Ce que je propose</h2>
  <div className="mt-8 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
    {/* 5 service cards, each with gold top border */}
  </div>
</Section>
```

Each service card structure:
```
<Card variant="on-ecru" className="border-t-3 border-or">
  <h3 className="font-serif text-xl font-bold">{title}</h3>
  <p className="mt-2 text-base leading-relaxed">{description}</p>
  <ul className="mt-3 space-y-1 text-base leading-relaxed">
    <li>— {sub-prestation 1}</li>
    <li>— {sub-prestation 2}</li>
    <li>— {sub-prestation 3}</li>
  </ul>
</Card>
```

The 5 service cards with content from `content/presentation_activite.md` section 3:

**Card 1: Facturation électronique**
- Description: (use a short intro sentence derived from the service context)
- Sub-prestations:
  - Accompagnement à la mise en conformité
  - Sélection et prise en main d'un logiciel adapté
  - Formation à l'émission et à la réception des factures électroniques

**Card 2: Outils numériques de gestion**
- Sub-prestations:
  - Diagnostic des besoins et recommandation d'outils simples
  - Mise en place d'un système de suivi clients et devis
  - Formation sur mesure aux outils sélectionnés

**Card 3: Présence en ligne locale**
- Sub-prestations:
  - Création et optimisation du profil Google Business
  - Accompagnement sur les réseaux sociaux professionnels (Facebook, LinkedIn)
  - Stratégie de visibilité locale adaptée aux petites structures

**Card 4: Formation aux outils d'intelligence artificielle**
- Sub-prestations:
  - Initiation aux outils IA accessibles (rédaction, traduction, images)
  - Applications concrètes pour les tâches administratives courantes
  - Ateliers en petit groupe ou accompagnement individuel

**Card 5: Création d'outils personnalisés**
- Sub-prestations:
  - Tableaux de bord simples adaptés à l'activité
  - Bases de données prospects et clients
  - Automatisation pour réduire les tâches répétitives

Key details:
- Cards use `on-ecru` variant (section bg is ecru, so cards are white on ecru)
- Gold top border: `border-t-3 border-or` added via Card's `className` prop
- Grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6` -- NO centering overrides. Cards 4 and 5 naturally sit in columns 1 and 2 of the last row, column 3 is empty (per locked decision).
- Bullet list items use em-dash "—" prefix (matching content source style), not HTML list markers
- All French accented characters preserved exactly
  </action>
  <verify>
    <automated>grep -q 'export function ServicesSection' src/components/sections/ServicesSection.tsx && grep -q 'id="services"' src/components/sections/ServicesSection.tsx && grep -q 'alternate' src/components/sections/ServicesSection.tsx && grep -c 'border-t-3 border-or' src/components/sections/ServicesSection.tsx | grep -q '5' && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - File exports `ServicesSection` as a named export
    - Contains `<Section id="services" alternate>`
    - Contains `<h2` with text "Ce que je propose" (H2 level)
    - Contains exactly 5 `<Card variant="on-ecru"` elements
    - Each Card has `className` containing `border-t-3 border-or` (gold top border accent)
    - Each Card contains `<h3`, a `<p` description, and a `<ul` with `<li` items
    - Card titles include: "Facturation électronique", "Outils numériques de gestion", "Présence en ligne locale", "Formation aux outils d'intelligence artificielle", "Création d'outils personnalisés"
    - Grid uses `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`
    - Grid does NOT contain `justify-items-center` or `justify-center` or `place-items-center`
    - Does NOT contain `"use client"` directive
  </acceptance_criteria>
  <done>ServicesSection renders 5 service cards with gold top border accent in responsive 1/2/3-column grid on ecru background, with left-aligned orphan cards</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- PourQuiSection.tsx has 4 Card components with on-white variant
- ServicesSection.tsx has 5 Card components with on-ecru variant and border-t-3 border-or
- `npm run lint` passes
</verification>

<success_criteria>
- Pour qui shows 4 text-only problem cards in 1-col mobile / 2x2 desktop grid on white background
- Services shows 5 domain cards with gold top border in 1/2/3 column responsive grid on ecru background
- Each service card has title + description + bullet list of sub-prestations
- Content matches presentation_activite.md with accented characters preserved
- No "use client" directives
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-sections/03-02-SUMMARY.md`
</output>
