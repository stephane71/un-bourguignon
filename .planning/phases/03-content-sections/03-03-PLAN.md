---
phase: 03-content-sections
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/sections/MethodeSection.tsx
  - src/components/sections/BeneficesSection.tsx
autonomous: true
requirements:
  - METH-01
  - METH-02
  - METH-03
  - METH-04
  - METH-05
  - BENE-01
  - BENE-02

must_haves:
  truths:
    - "Methode shows a 3-step stepper (vertical mobile, horizontal desktop) with gold step numbers"
    - "Methode shows a formats table with 4 rows (consultation, mission, atelier, suivi)"
    - "Benefices shows 4 items each with a thin-line SVG icon, Lora title, and Instrument Sans text"
  artifacts:
    - path: "src/components/sections/MethodeSection.tsx"
      provides: "Methode section with stepper + formats table"
      exports: ["MethodeSection"]
      min_lines: 60
    - path: "src/components/sections/BeneficesSection.tsx"
      provides: "Benefices section with 4 icon+title+text items"
      exports: ["BeneficesSection"]
      min_lines: 40
  key_links:
    - from: "src/components/sections/BeneficesSection.tsx"
      to: "src/components/icons/Clock.tsx"
      via: "import Clock icon"
      pattern: "import.*Clock.*from.*icons/Clock"
    - from: "src/components/sections/BeneficesSection.tsx"
      to: "src/components/icons/Key.tsx"
      via: "import Key icon"
      pattern: "import.*Key.*from.*icons/Key"
    - from: "src/components/sections/BeneficesSection.tsx"
      to: "src/components/icons/UserCheck.tsx"
      via: "import UserCheck icon"
      pattern: "import.*UserCheck.*from.*icons/UserCheck"
    - from: "src/components/sections/BeneficesSection.tsx"
      to: "src/components/icons/CoinStack.tsx"
      via: "import CoinStack icon"
      pattern: "import.*CoinStack.*from.*icons/CoinStack"
---

<objective>
Build the Methode (3-step stepper + formats table) and Benefices (4 icon+title+text items) sections -- the trust-building narrative arc.

Purpose: Methode explains how Stephane works (process transparency). Benefices shows concrete client gains (outcome focus). These sections depend on icon components from Plan 01.
Output: 2 section components in src/components/sections/
</objective>

<execution_context>
@/Users/stephane/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stephane/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-content-sections/03-CONTEXT.md
@.planning/phases/03-content-sections/03-UI-SPEC.md
@content/presentation_activite.md
@.planning/phases/03-content-sections/03-01-SUMMARY.md

<interfaces>
From src/components/ui/Section.tsx:
```typescript
interface SectionProps {
  id: string;
  children: React.ReactNode;
  alternate?: boolean;
  className?: string;
}
export function Section({ id, children, alternate = false, className = "" }: SectionProps)
```

From src/components/icons/ (created in Plan 01):
```typescript
// All icons follow this pattern:
export function Clock({ className = "", size = 24 }: { className?: string; size?: number })
export function Key({ className = "", size = 24 }: { className?: string; size?: number })
export function UserCheck({ className = "", size = 24 }: { className?: string; size?: number })
export function CoinStack({ className = "", size = 24 }: { className?: string; size?: number })
// All render stroke-based SVGs with aria-hidden="true"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MethodeSection component</name>
  <files>src/components/sections/MethodeSection.tsx</files>
  <read_first>
    src/components/ui/Section.tsx
    content/presentation_activite.md
    .planning/phases/03-content-sections/03-UI-SPEC.md
  </read_first>
  <action>
Create `src/components/sections/MethodeSection.tsx` as a Server Component (no `"use client"`).

Import Section from `@/components/ui/Section`.

Section uses `id="methode"` with NO `alternate` prop (white background -- 5th section).

The section has two sub-blocks:

**Sub-block 1: Stepper (3 steps)**

Define steps data inline:
```typescript
const steps = [
  {
    title: "Diagnostic terrain",
    description: "Je commence toujours par comprendre. Un premier échange — souvent sur le terrain ou par téléphone — me permet de cerner la situation réelle : quels outils sont déjà utilisés, quels sont les blocages, quelles sont les priorités. Pas de solution préfabriquée : chaque diagnostic est unique."
  },
  {
    title: "Accompagnement personnalisé",
    description: "Je propose un plan d'action concret, adapté à la réalité du client. L'accompagnement peut prendre la forme de séances de formation, d'une mise en place d'outils avec formation incluse, ou d'un suivi régulier sur plusieurs semaines."
  },
  {
    title: "Suivi et autonomie",
    description: "L'objectif final est l'autonomie du client. Je fournis des supports simples, des guides adaptés, et reste disponible pour les questions après la mission."
  },
];
```

Stepper layout:
```
<div className="mt-8 flex flex-col lg:flex-row lg:items-start gap-8 lg:gap-0">
  {steps.map((step, i) => (
    <div key={i} className="flex-1 relative lg:px-6 first:lg:pl-0 last:lg:pr-0">
      {/* Horizontal connector line on desktop (not on first step) */}
      {i > 0 && (
        <div className="hidden lg:block absolute top-6 left-0 w-6 h-0.5 bg-sable -translate-x-full" />
      )}
      {/* Vertical connector on mobile (not on first step) */}
      {i > 0 && (
        <div className="lg:hidden w-0.5 h-8 bg-sable ml-5 -mt-8 mb-0" />
      )}
      <span className="font-serif font-bold text-or-light text-4xl lg:text-5xl">
        {i + 1}
      </span>
      <h3 className="font-serif text-xl font-bold mt-2">{step.title}</h3>
      <p className="text-base leading-relaxed mt-2">{step.description}</p>
    </div>
  ))}
</div>
```

Step numbers use: `font-serif font-bold text-or-light text-4xl lg:text-5xl` (Lora Bold, gold #D4A574, 36px mobile, 48px desktop -- per METH-03).

Connector design: thin sable-colored lines between steps. Horizontal on desktop (hidden lg:block), vertical on mobile (lg:hidden). These are decorative -- simple approach with absolute positioned divs.

**Sub-block 2: Formats table**

Define formats data inline:
```typescript
const formats = [
  { name: "Consultation ponctuelle", duration: "1 à 2 heures", ideal: "Question précise, déblocage rapide" },
  { name: "Mission d'accompagnement", duration: "3 à 10 séances", ideal: "Mise en place d'un outil ou d'une conformité" },
  { name: "Atelier collectif", duration: "1/2 journée", ideal: "Formation en petit groupe (IA, outils numériques)" },
  { name: "Suivi mensuel", duration: "1 à 2 h / mois", ideal: "Maintien des compétences, questions au fil de l'eau" },
];
```

Table layout:
```
<div className="mt-12 lg:mt-16">
  <h3 className="font-serif text-xl font-bold">Formats d'intervention</h3>
  <div className="overflow-x-auto mt-4">
    <table className="w-full text-left">
      <thead>
        <tr className="border-b-2 border-sable">
          <th scope="col" className="font-serif font-bold py-3 px-4">Format</th>
          <th scope="col" className="font-serif font-bold py-3 px-4">Durée</th>
          <th scope="col" className="font-serif font-bold py-3 px-4">Idéal pour</th>
        </tr>
      </thead>
      <tbody>
        {formats.map((f, i) => (
          <tr key={i} className="border-b border-sable">
            <td className="py-3 px-4 font-bold">{f.name}</td>
            <td className="py-3 px-4">{f.duration}</td>
            <td className="py-3 px-4">{f.ideal}</td>
          </tr>
        ))}
      </tbody>
    </table>
  </div>
</div>
```

Table uses semantic HTML: `<thead>`, `<th scope="col">`, `<tbody>`, `<tr>`, `<td>`. Wrapped in `overflow-x-auto` for horizontal scroll on small screens. All French accented characters preserved from content source.
  </action>
  <verify>
    <automated>grep -q 'export function MethodeSection' src/components/sections/MethodeSection.tsx && grep -q 'id="methode"' src/components/sections/MethodeSection.tsx && grep -q 'text-or-light' src/components/sections/MethodeSection.tsx && grep -q '<table' src/components/sections/MethodeSection.tsx && grep -q 'scope="col"' src/components/sections/MethodeSection.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - File exports `MethodeSection` as a named export
    - Contains `<Section id="methode">` with NO `alternate` prop (white background)
    - Contains `<h2` with text "Ma méthode de travail" (H2 level)
    - Stepper contains 3 steps with step numbers using `text-or-light text-4xl lg:text-5xl font-serif font-bold`
    - Step titles are: "Diagnostic terrain", "Accompagnement personnalisé", "Suivi et autonomie"
    - Contains connector elements (sable-colored lines) between steps
    - Contains `lg:flex-row` for horizontal desktop layout and `flex-col` for vertical mobile
    - Contains a `<table` element with `<thead>`, `<th scope="col">`, `<tbody>`
    - Table has 3 columns: "Format", "Durée", "Idéal pour"
    - Table has 4 data rows: "Consultation ponctuelle", "Mission d'accompagnement", "Atelier collectif", "Suivi mensuel"
    - Table is wrapped in `overflow-x-auto`
    - Does NOT contain `"use client"` directive
  </acceptance_criteria>
  <done>MethodeSection renders 3-step stepper with gold numbers (vertical/horizontal responsive) and a 4-row formats table with semantic HTML</done>
</task>

<task type="auto">
  <name>Task 2: Create BeneficesSection component</name>
  <files>src/components/sections/BeneficesSection.tsx</files>
  <read_first>
    src/components/icons/Clock.tsx
    src/components/icons/Key.tsx
    src/components/icons/UserCheck.tsx
    src/components/icons/CoinStack.tsx
    src/components/ui/Section.tsx
    content/presentation_activite.md
    .planning/phases/03-content-sections/03-UI-SPEC.md
  </read_first>
  <action>
Create `src/components/sections/BeneficesSection.tsx` as a Server Component (no `"use client"`).

Import Section from `@/components/ui/Section`, and the 4 benefit icons: Clock, Key, UserCheck, CoinStack from `@/components/icons/{name}`.

Section uses `id="benefices"` with `alternate` prop (ecru background -- 6th section).

Structure:
```
<Section id="benefices" alternate>
  <h2 className="font-serif text-xl lg:text-4xl font-bold">Ce que vous gagnez</h2>
  <p className="mt-4 text-base leading-relaxed max-w-3xl">
    Travailler avec moi, c'est choisir un accompagnement qui produit des résultats
    mesurables dans votre quotidien professionnel — pas des promesses générales.
  </p>
  <div className="mt-8 grid grid-cols-1 md:grid-cols-2 gap-8">
    {/* Benefit 1: Time saved */}
    <div>
      <Clock size={32} className="text-brun" />
      <h3 className="font-serif text-xl font-bold mt-3">Du temps récupéré sur l'administratif</h3>
      <p className="text-base leading-relaxed mt-2">
        Chaque outil bien configuré, chaque processus simplifié, c'est du temps rendu au
        métier. Que ce soit la facturation automatisée, un tableau de suivi efficace ou une
        présence en ligne qui fonctionne sans intervention quotidienne — l'objectif est
        toujours de réduire la charge administrative invisible.
      </p>
    </div>
    {/* Benefit 2: Autonomy */}
    <div>
      <Key size={32} className="text-brun" />
      <h3 className="font-serif text-xl font-bold mt-3">L'autonomie, pas la dépendance</h3>
      <p className="text-base leading-relaxed mt-2">
        Une mission réussie, c'est un client qui n'a plus besoin de moi pour les gestes
        du quotidien. Je forme, j'explique, je fournis des supports clairs. Vous repartez
        avec les clés en main.
      </p>
    </div>
    {/* Benefit 3: Single contact */}
    <div>
      <UserCheck size={32} className="text-brun" />
      <h3 className="font-serif text-xl font-bold mt-3">Un interlocuteur unique qui comprend votre métier</h3>
      <p className="text-base leading-relaxed mt-2">
        Pas de hotline, pas de ticket de support. Un seul interlocuteur, disponible, qui
        connaît votre activité et votre contexte. Vous n'avez pas à réexpliquer à chaque
        fois — le suivi est personnalisé et continu.
      </p>
    </div>
    {/* Benefit 4: Fair pricing */}
    <div>
      <CoinStack size={32} className="text-brun" />
      <h3 className="font-serif text-xl font-bold mt-3">La certitude de payer juste</h3>
      <p className="text-base leading-relaxed mt-2">
        Devis clair avant toute intervention, tarif adapté à la taille et aux besoins réels
        de votre structure. Pas de forfait surdimensionné, pas de surprise en fin de mission.
        Vous savez ce que vous achetez.
      </p>
    </div>
  </div>
</Section>
```

Icon mapping (per UI-SPEC):
- Clock (32px, text-brun) = "Du temps récupéré sur l'administratif" (time saved)
- Key (32px, text-brun) = "L'autonomie, pas la dépendance" (autonomy)
- UserCheck (32px, text-brun) = "Un interlocuteur unique qui comprend votre métier" (single contact)
- CoinStack (32px, text-brun) = "La certitude de payer juste" (fair pricing)

Layout: `grid grid-cols-1 md:grid-cols-2 gap-8` -- 2x2 grid on desktop, stacked on mobile. Each item stacks icon above title above text. Icons use `size={32}` and `className="text-brun"`. All content from `content/presentation_activite.md` section 5.
  </action>
  <verify>
    <automated>grep -q 'export function BeneficesSection' src/components/sections/BeneficesSection.tsx && grep -q 'id="benefices"' src/components/sections/BeneficesSection.tsx && grep -q 'alternate' src/components/sections/BeneficesSection.tsx && grep -q 'Clock' src/components/sections/BeneficesSection.tsx && grep -q 'Key' src/components/sections/BeneficesSection.tsx && grep -q 'UserCheck' src/components/sections/BeneficesSection.tsx && grep -q 'CoinStack' src/components/sections/BeneficesSection.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - File exports `BeneficesSection` as a named export
    - Contains `<Section id="benefices" alternate>`
    - Contains `<h2` with text "Ce que vous gagnez" (H2 level)
    - Imports Clock, Key, UserCheck, CoinStack from `@/components/icons/` paths
    - Contains 4 benefit items, each with an icon component (`size={32}`), `<h3` title, and `<p` description
    - Benefit titles: "Du temps récupéré sur l'administratif", "L'autonomie, pas la dépendance", "Un interlocuteur unique qui comprend votre métier", "La certitude de payer juste"
    - Icons use `className="text-brun"` for coloring
    - Grid uses `grid grid-cols-1 md:grid-cols-2 gap-8`
    - Does NOT contain `"use client"` directive
  </acceptance_criteria>
  <done>BeneficesSection renders 4 benefit items with thin-line SVG icons, Lora titles, and Instrument Sans descriptions in a 2x2 grid on ecru background</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- MethodeSection.tsx contains stepper with 3 steps and formats table with 4 rows
- BeneficesSection.tsx imports all 4 benefit icons and renders 4 items
- `npm run lint` passes
</verification>

<success_criteria>
- Methode stepper shows 3 steps with gold Lora Bold numbers, vertical on mobile, horizontal on desktop
- Methode formats table has 4 rows with semantic thead/th/tbody and overflow-x-auto
- Benefices shows 4 items with Clock/Key/UserCheck/CoinStack icons at 32px
- Content matches presentation_activite.md with accented characters
- No "use client" directives
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-sections/03-03-SUMMARY.md`
</output>
